import { Province, Municipality, District } from '../index';

export class Address {
  sector: string;
  street: string;
  number: string;

  provinceName: string;
  municipalityName: string;
  districtName: string;
}
